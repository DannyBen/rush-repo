#!/usr/bin/env bash
source ../common.sh

if command_exist svg-term ; then
  say "svg-term is already installed"

else

  say "installing prerequisites"
  needs yarn asciinema

  say "installing svg-term-cli (yarn)"
  sudo yarn global add svg-term-cli

  say "svg-term installation complete"

fi
