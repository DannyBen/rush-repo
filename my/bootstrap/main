#!/usr/bin/env bash
set -e

rush $REPO bashrc
rush $REPO dircolors
rush $REPO exrc
rush $REPO inputrc
rush $REPO irbrc
[[ -x "$(command -v alf)" ]] || rush $REPO alf

if [[ $RUSH_MODE == "personal" ]]; then
  rush gitconfig
  rush cdpath
fi

printf "\nDone. You may need to login again.\n"
