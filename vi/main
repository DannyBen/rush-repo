#!/usr/bin/env bash
source ../common.sh

case "$DISTRO" in
  "ubuntu")
    attention "not needed for Ubuntu"
    ;;
  "arch")
    package_install vim
    say "copying ~/.bashrc.d/vi.bashrc"
    cp vi.bashrc "$HOME/.bashrc.d/"
    ;;

esac

