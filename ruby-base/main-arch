#!/usr/bin/env bash
source "$REPO_PATH/lib.sh"

sudo pacman -Syu && sudo pacman -S --noconfirm --needed \
  base-devel \
  curl \
  gdbm \
  git \
  libffi \
  libyaml \
  ncurses \
  openssl \
  readline \
  wget \
  zlib
