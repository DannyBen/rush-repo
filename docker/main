#!/usr/bin/env bash
source ../common.sh

say "installing docker (setuip script)"
curl https://get.docker.com | sudo sh
sudo usermod -aG docker "$(whoami)"
sudo service docker start
say "docker installation complete"
