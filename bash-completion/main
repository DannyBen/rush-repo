#!/usr/bin/env bash
source "$REPO_PATH/lib.sh"

if [[ "$DISTRO" == "arch" ]]; then
  package_install bash-completion
fi

say "configuring user bash completions"
cp .bash_completion ~/
mkdir -p ~/.bash_completion.d
attention "restart your session or source ~/.bash_completion"
